{{- if not .Values.coturn.auth.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-config
  labels:
{{ include "labels" . | nindent 4}}
data:
  user.cfg: {{ printf "user=%s:%s" .Values.coturn.auth.username .Values.coturn.auth.password | b64enc }}
{{- end }}
